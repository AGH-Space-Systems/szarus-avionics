/*
get the orthogon going
*/

/*
  //float currentAltitude = bmp.readAltitude();
  //Serial.println(bmp.readAltitude());
  delay(200);

  if(currentAltitude>=maxAltitude)
  {
    maxAltitude=currentAltitude;
  }
  else
  {
    if (apogeeTimer.hasPassed(100)){
      Serial.println("Wyjebalo sie");
    }
  }
*/
 //Serial.println("START--");

   //m/s
  //Serial.println("Acc x:");
  //Serial.println(acc.acceleration.x);
  //Serial.println("Acc y:");
  //Serial.println(acc.acceleration.y);
  //Serial.println("Acc z:");
  //Serial.println(acc.acceleration.z);
  //Serial.println("Wypadkowe a:");
  //Serial.println(vectorLength(acc.acceleration.x, acc.acceleration.y, acc.acceleration.z));
